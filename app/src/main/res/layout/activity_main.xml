<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <TextView
        android:id="@+id/txtResult"
        android:layout_width="600px"
        android:layout_height="300px"

        android:text="Hello World!"
        app:layout_constraintRight_toRightOf="@+id/imgResult"
        app:layout_constraintTop_toBottomOf="@+id/imgResult" />

    <TextView
        android:id="@+id/txtResult_line"
        android:layout_width="600px"
        android:layout_height="300px"

        android:text="Hello World!"
        app:layout_constraintRight_toRightOf="@+id/txtResult"
        app:layout_constraintTop_toBottomOf="@+id/txtResult" />

    <ImageView
        android:id="@+id/imgResult"
        android:layout_width="1200px"
        android:layout_height="1300px"
        android:layout_gravity = "center"
        android:adjustViewBounds="true"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"

        />

    <Button
        android:id="@+id/btnGallery"
        android:layout_width="400px"
        android:layout_height="200px"
        android:onClick="GallaryOnClick"
        android:text="GALLERY"
        app:layout_constraintRight_toLeftOf="@+id/txtResult"
        app:layout_constraintTop_toBottomOf="@+id/imgResult" />

</androidx.constraintlayout.widget.ConstraintLayout>